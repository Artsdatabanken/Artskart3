# ArtKart3 robots.txt
# Note: robots.txt is served dynamically by middleware based on environment:
# Development/Test: Disallow all (blocks crawlers)
# Production: Allow all (permits crawlers)

User-agent: *
Allow: /
